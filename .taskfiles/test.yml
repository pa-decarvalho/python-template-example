---
version: "3"

tasks:
  pytest:
    desc: Run pytest command
    silent: true
    cmds:
      - poetry run pytest -vv

  all:
    desc: Run all test
    silent: true
    cmds:
      - task: pytest
